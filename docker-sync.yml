version: "2"

options:
  verbose: true
syncs:
  PROJECTNAME-sync:
    src: '.'
    host_disk_mount_mode: 'cached'
  PROJECTNAME-uploads-sync:
    src: './.docker/uploads'
    host_disk_mount_mode: 'cached'