version: "2"

options:
  verbose: true
syncs:
  thedustpresstheme-sync:
    src: '.'
    host_disk_mount_mode: 'cached'
  thedustpresstheme-uploads-sync:
    src: './.docker-sync/uploads'
    host_disk_mount_mode: 'cached'
